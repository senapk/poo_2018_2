#include <iostream>
#include <iomanip>
#include <sstream>
using namespace std;
/*  setw, setprecision
    static method
    static variable
    varias coisas do fluxo
*/

struct Fone{
    string id;
    string number;

    static bool validate(string number){
        blsadfds;
        ret;
    }
};

int main(){
    Fone f("oi", "54361a");
    f.validate();

    Fone::validate("54361a");

    Interacoes::forcaMagnetica(corpoa, corpob);
    R


    cout << "x" << setw(10) << "ovo;";

    printf("%07.2f", number);
    cout << fixed << setprecision(2) << 2344.5932489 << endl;

    //mary vivo:1234 oi:4321 work:4444

    string line;
    getline(cin, line);
    stringstream ss(line);
    string word;
    while(ss >> word){
        stringstream ss(word);
        string nome, fone;
        getline(ss, nome, ':');
        getline(ss, fone);
    }

    return 0;
}
